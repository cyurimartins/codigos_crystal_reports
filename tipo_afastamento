IF ( ({Comando.R8_DATAINI} <> '' AND {Comando.R8_DATAFIM} = '') OR ({Comando.R8_DATAFIM} > totext(currentdate,'yyyyMMdd')) ) THEN
IF {Comando.R8_TIPO} ='1' THEN
	'APOSENT. POR INVALIDEZ'
ELSE IF {Comando.R8_TIPO} ='2' THEN
	'RESC. POR CULPA RECIPROCA OU FORCA MAIOR'
ELSE IF {Comando.R8_TIPO} ='3' THEN
	'RESC. POR FINALIZACAO DO CONTRATO A TERMO'
ELSE IF {Comando.R8_TIPO} ='4' THEN
	'RESC. S/JUSTA CAUSA EMPREG. DOMESTICO P/EMPREGADOR'
ELSE IF {Comando.R8_TIPO} ='5' THEN
	'TRANSF.P/OUTRO ESTABELECIMENTO DA MESMA EMPRESA'
ELSE IF {Comando.R8_TIPO} ='6' THEN
	'LIC. MATERNIDADE POR ADOCAO(60 dias)'
ELSE IF {Comando.R8_TIPO} ='7' THEN
	'LIC. MATERNIDADE POR ADOCAO(30 dias)'
ELSE IF {Comando.R8_TIPO} ='8' THEN
	'LIC. REMUNERADA'
ELSE IF {Comando.R8_TIPO} ='9' THEN
	'FALEC. MOTIVADO POR ACID. DO TRABALHO'
ELSE IF {Comando.R8_TIPO} ='A' THEN
	'AFAST. TEMPORARIO'
ELSE IF {Comando.R8_TIPO} ='B     ' THEN
	'LIC. MATERN. POR ADOCAO(120 dias)'
ELSE IF {Comando.R8_TIPO} ='D     ' THEN
	'RECESSO DE ESTAGIARIO'
ELSE IF {Comando.R8_TIPO} ='F' THEN
	'FERIAS'
ELSE IF {Comando.R8_TIPO} ='H' THEN
	'RESC. COM JUSTA CAUSA INICI. EMPREG.'
ELSE IF {Comando.R8_TIPO} ='I' THEN
	'RESC. SEM JUSTA CAUSA INICI. EMPREG.'
ELSE IF {Comando.R8_TIPO} ='J' THEN
	'DESLIG. DO EMPREGADO INICI. PROP.'
ELSE IF {Comando.R8_TIPO} ='K' THEN
	'RESC. A PEDIDO DO EMPREG.,INICI. PROPRIA'
ELSE IF {Comando.R8_TIPO} ='L' THEN
	'OUTROS MOTIVOS DE RESC. DE CONTRATO DE TRAB.'
ELSE IF {Comando.R8_TIPO} ='M' THEN
	'MUDANCA PARA REGIME ESTATUTARIO'
ELSE IF {Comando.R8_TIPO} ='N' THEN
	'SAIDA PARA OUTRO ESTABELEC. SEM RESC. TRAB.'
ELSE IF {Comando.R8_TIPO} ='O' THEN
	'AFAST. TEMP. POR ACIDENTE DE TRABALHO'
ELSE IF {Comando.R8_TIPO} ='P' THEN
	'AFAST. TEMP. POR MOTIVO DE DOENCA'
ELSE IF {Comando.R8_TIPO} ='Q' THEN
	'AFAST. TEMP. POR MOTIVO LIC. GESTANTE'
ELSE IF {Comando.R8_TIPO} ='R' THEN
	'AFAST. TEMP. PARA PREST. SERVICO MILITAR'
ELSE IF {Comando.R8_TIPO} ='S' THEN
	'FALECIMENTO'
ELSE IF {Comando.R8_TIPO} ='U' THEN
	'APOSENT. COM RESCISAO DE CONTRATO'
ELSE IF {Comando.R8_TIPO} ='W' THEN
	'AFAST. TEMP. MANDATO SINDICAL'
ELSE IF {Comando.R8_TIPO} ='X' THEN
	'LICENCA SEM VENCIMENTOS'
ELSE IF {Comando.R8_TIPO} ='Y' THEN
	'OUTROS MOTIVOS DE AFAST. TEMP.'
ELSE IF {Comando.R8_TIPO} ='Z' THEN
	'RETORNO DE AFAST. TEMP. E/OU LICENCA'
